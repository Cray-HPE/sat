# Example of a valid SAT config file.
# Copyright 2019 Cray Inc. All Rights Reserved
#
# Note that the values are all different from the defaults, which tests that
# the values are really being set from the config file.
[logging]
file_name = "/var/log/sat.log"
file_level = "DEBUG"
stderr_level = "ERROR"
